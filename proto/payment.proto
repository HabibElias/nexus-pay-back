syntax = "proto3";

package payment;

option go_package = "web3-payments/backend/payment-service/generated/proto";

message CreatePaymentRequest {
  double amount = 1;
}

message Payment {
  string id = 1;
  double amount = 2;
}

service PaymentService {
  rpc CreatePayment(CreatePaymentRequest) returns (Payment);
}
